import { makeStyles } from '@material-ui/core/styles';

export default makeStyles((color) => ({
    boxShadow: `1px 2px 2px ${color},2px 4px 4px ${color},4px 8px 8px ${color},8px 16px 16px ${color},16px 32px 32px ${color} !important`

  }));